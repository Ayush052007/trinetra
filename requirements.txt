# TRINETRA backend dependencies
# Verified compatible with Python 3.14 (cp314 wheels or pure-Python).

fastapi==0.141.1
uvicorn[standard]==0.52.4
sqlalchemy==2.0.52
alembic==1.19.1
pydantic==2.12.4
pydantic-settings==2.12.0
PyJWT==2.13.0
python-multipart==0.0.32
fpdf2==2.8.8
websockets==15.0.1

# Optional graph backend. The platform runs on the embedded engine by default;
# install/enable this only when a Neo4j instance is available.
neo4j==6.3.0

# PostgreSQL driver. Not needed for the default SQLite setup, but required
# for any cloud deployment - see docs/DEPLOYMENT.md.
psycopg[binary]==3.3.5

# Test tooling
pytest==8.4.2
httpx==0.28.1
